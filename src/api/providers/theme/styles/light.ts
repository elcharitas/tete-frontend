import { teteBlackDark, teteBlue } from "../colors";
import { TThemePalette } from "../types";

export const lightTheme: TThemePalette = {
    mode: "light",
    primary: {
        main: teteBlue,
        dark: teteBlackDark,
    },
};
